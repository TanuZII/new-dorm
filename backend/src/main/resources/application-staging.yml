server:
  servlet:
    session:
      cookie:
        secure: true

management:
  endpoint:
    health:
      probes:
        enabled: true

logging:
  structured:
    format:
      console: logstash
